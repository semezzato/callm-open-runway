{
  "name": "callm-monorepo",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "caLLM / Open Runway - Universal LLM Orchestrator Monorepo",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "cli:dev": "npm run dev -w apps/cli",
    "cli:build": "npm run build -w apps/cli",
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@vitest/ui": "^4.1.2",
    "happy-dom": "^20.8.8",
    "jsdom": "^29.0.1",
    "playwright": "^1.58.2",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.0",
    "vitest": "^4.1.2"
  }
}
